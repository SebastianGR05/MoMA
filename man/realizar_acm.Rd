% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ACM_funcion.R
\name{realizar_acm}
\alias{realizar_acm}
\title{Realizar Análisis de Correspondencia Múltiple (ACM) con FactoMineR y Factoextra}
\usage{
realizar_acm(
  datos,
  ncp = 5,
  quali.sup = NULL,
  quanti.sup = NULL,
  mostrar_eigen = TRUE,
  grafico_scree = TRUE,
  grafico_var = TRUE,
  ...
)
}
\arguments{
\item{datos}{Un data frame que contiene las variables. Las variables a incluir
en el análisis activo deben ser de tipo factor.}

\item{ncp}{Número de dimensiones a conservar en los resultados. Por defecto 5.}

\item{quali.sup}{Un vector de índices o nombres de columnas categóricas
(factores) que se usarán como variables suplementarias. Por defecto NULL.}

\item{quanti.sup}{Un vector de índices o nombres de columnas numéricas
que se usarán como variables suplementarias. Por defecto NULL.}

\item{mostrar_eigen}{Booleano. ¿Mostrar la tabla de eigenvalues (varianza explicada)
usando \code{factoextra::get_eigenvalue}? Por defecto TRUE.}

\item{grafico_scree}{Booleano. ¿Mostrar el gráfico de sedimentación (scree plot)
usando \code{factoextra::fviz_screeplot}? Por defecto TRUE.}

\item{grafico_var}{Booleano. ¿Mostrar el gráfico de variables (categorías)
usando \code{factoextra::fviz_mca_var} coloreado por contribución? Por defecto TRUE.}

\item{...}{Argumentos adicionales pasados a \code{FactoMineR::MCA}.}
}
\value{
Un objeto de clase \code{MCA} que contiene todos los resultados del análisis,
tal como lo devuelve \code{FactoMineR::MCA}. Devuelve \code{NULL} e imprime una advertencia
si no hay suficientes variables activas (factores no suplementarios).
Además, muestra los gráficos y tablas solicitados en los argumentos.
}
\description{
Esta función realiza un ACM sobre las variables categóricas (factores)
de un conjunto de datos utilizando \code{FactoMineR::MCA} y opcionalmente genera
visualizaciones mejoradas con \code{factoextra}.
Permite especificar variables categóricas y cuantitativas suplementarias.
}
\examples{
\dontrun{
# Cargar datos 'tea' de FactoMineR
if (requireNamespace("FactoMineR", quietly = TRUE)) {
  data(tea, package = "FactoMineR")

  # Ejecutar ACM especificando algunas variables suplementarias
  # (ej. Price category como cualitativa sup, age como cuantitativa sup)
  # Asegurarse de que las variables activas sean factores
  # Convertimos columnas necesarias a factor (si no lo son ya)
  cols_to_factor <- names(tea)[c(1:18, 20:36)] # Nombres de columnas categóricas y quali.sup
  for(col in cols_to_factor) {
     if(!is.factor(tea[[col]])) tea[[col]] <- factor(tea[[col]])
  }

  resultado_acm_mejorado <- realizar_acm(
    datos = tea,
    ncp = 5,
    quali.sup = 20:36, # Índices de cualitativas suplementarias
    quanti.sup = 19,   # Índice de cuantitativa suplementaria (Age)
    grafico_scree = TRUE,
    grafico_var = TRUE
  )

  # Acceder a los resultados
  summary(resultado_acm_mejorado)

} else {
  print("Los paquetes FactoMineR y factoextra son necesarios para este ejemplo.")
}
}

}
