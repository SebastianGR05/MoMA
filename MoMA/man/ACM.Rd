% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ACM.R
\name{ACM}
\alias{ACM}
\title{Análisis de Correspondencia Múltiple (ACM)}
\usage{
ACM(
  datos,
  ncp = 5,
  quali.sup = NULL,
  quanti.sup = NULL,
  mostrar_eigen = TRUE,
  grafico_scree = TRUE,
  grafico_var = TRUE,
  ...
)
}
\arguments{
\item{datos}{Un data frame que contiene las variables.}

\item{ncp}{Número de dimensiones a conservar. Por defecto 5.}

\item{quali.sup}{Vector de índices o nombres de columnas categóricas suplementarias.}

\item{quanti.sup}{Vector de índices o nombres de columnas numéricas suplementarias.}

\item{mostrar_eigen}{Booleano. ¿Mostrar tabla de eigenvalues? Por defecto TRUE.}

\item{grafico_scree}{Booleano. ¿Mostrar scree plot? Por defecto TRUE.}

\item{grafico_var}{Booleano. ¿Mostrar gráfico de variables? Por defecto TRUE.}

\item{...}{Argumentos adicionales pasados a \code{FactoMineR::MCA}.}
}
\value{
Un objeto de clase \code{MCA}. Devuelve \code{NULL} si no hay suficientes variables
activas válidas. Muestra gráficos y tablas solicitados.
}
\description{
Realiza un ACM utilizando \code{FactoMineR::MCA}, usando solo las filas
completas para las variables activas (factores no suplementarios).
Muestra opcionalmente gráficos mejorados con \code{factoextra}.
}
\examples{
\dontrun{
# Cargar datos 'tea' y añadir algunos NAs
if (requireNamespace("FactoMineR", quietly = TRUE)) {
  data(tea, package = "FactoMineR")
  set.seed(123)
  tea$Tea[sample(1:nrow(tea), 20)] <- NA # Añadir NAs en variable activa
  tea$How[sample(1:nrow(tea), 15)] <- NA # Añadir NAs en variable activa
  tea$age[sample(1:nrow(tea), 10)] <- NA # Añadir NAs en variable quanti.sup

  # Convertir a factores si es necesario (como antes)
  cols_to_factor <- names(tea)[!(names(tea) \%in\% c("age"))]
  for(col in cols_to_factor) {
     if(!is.factor(tea[[col]])) tea[[col]] <- factor(tea[[col]])
  }

  # Ejecutar ACM (manejará NAs en activas; puede usar NAs en suplementarias)
  resultado_acm_na <- ACM(
    datos = tea,
    ncp = 5,
    quali.sup = c("where", "always"), # Nombres o índices
    quanti.sup = "age",
    grafico_var = FALSE # Desactivar para ejemplo rápido
  )
  if(!is.null(resultado_acm_na)) summary(resultado_acm_na)

} else {
  print("Se necesita FactoMineR y factoextra.")
}
}

}
